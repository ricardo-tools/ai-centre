'use client';

import type { RenderableWidget } from '@/screen-renderer/types';
import { PresentationShowcase } from '@/app/skills/[slug]/showcases/PresentationShowcase';

export function PresentationShowcaseLG(_props: RenderableWidget) {
  return <PresentationShowcase />;
}
