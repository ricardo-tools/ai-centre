'use client';

import type { RenderableWidget } from '@/platform/screen-renderer/types';
import { UserManagementShowcase } from '@/features/skill-detail/showcases/UserManagementShowcase';

export function UserManagementShowcaseLG(_props: RenderableWidget) {
  return <UserManagementShowcase />;
}
