'use client';

import type { RenderableWidget } from '@/platform/screen-renderer/types';
import { PptxExportShowcase } from '@/app/skills/[slug]/showcases/PptxExportShowcase';

export function PptxExportShowcaseLG(_props: RenderableWidget) {
  return <PptxExportShowcase />;
}
