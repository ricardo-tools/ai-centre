'use client';

import type { RenderableWidget } from '@/platform/screen-renderer/types';
import { FrontendArchitectureShowcase } from '@/app/skills/[slug]/showcases/FrontendArchitectureShowcase';

export function FrontendArchitectureShowcaseLG(_props: RenderableWidget) {
  return <FrontendArchitectureShowcase />;
}
