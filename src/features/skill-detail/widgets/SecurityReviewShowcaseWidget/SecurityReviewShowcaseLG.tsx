'use client';

import type { RenderableWidget } from '@/platform/screen-renderer/types';
import { SecurityReviewShowcase } from '@/features/skill-detail/showcases/SecurityReviewShowcase';

export function SecurityReviewShowcaseLG(_props: RenderableWidget) {
  return <SecurityReviewShowcase />;
}
