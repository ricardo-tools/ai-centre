'use client';

import type { RenderableWidget } from '@/platform/screen-renderer/types';
import { InteractionMotionShowcase } from '@/features/skill-detail/showcases/InteractionMotionShowcase';

export function InteractionMotionShowcaseLG(_props: RenderableWidget) {
  return <InteractionMotionShowcase />;
}
