'use client';

import type { RenderableWidget } from '@/platform/screen-renderer/types';
import { FlowShowcase } from '@/features/skill-detail/showcases/FlowShowcase';

export function FlowShowcaseLG(_props: RenderableWidget) {
  return <FlowShowcase />;
}
