'use client';

import type { RenderableWidget } from '@/platform/screen-renderer/types';
import { ObservabilityShowcase } from '@/features/skill-detail/showcases/ObservabilityShowcase';

export function ObservabilityShowcaseLG(_props: RenderableWidget) {
  return <ObservabilityShowcase />;
}
