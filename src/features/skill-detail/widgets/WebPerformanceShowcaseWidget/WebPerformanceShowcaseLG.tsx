'use client';

import type { RenderableWidget } from '@/platform/screen-renderer/types';
import { WebPerformanceShowcase } from '@/features/skill-detail/showcases/WebPerformanceShowcase';

export function WebPerformanceShowcaseLG(_props: RenderableWidget) {
  return <WebPerformanceShowcase />;
}
