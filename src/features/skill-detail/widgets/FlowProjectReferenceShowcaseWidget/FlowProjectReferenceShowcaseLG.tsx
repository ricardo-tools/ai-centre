'use client';

import type { RenderableWidget } from '@/platform/screen-renderer/types';
import { FlowProjectReferenceShowcase } from '@/features/skill-detail/showcases/FlowProjectReferenceShowcase';

export function FlowProjectReferenceShowcaseLG(_props: RenderableWidget) {
  return <FlowProjectReferenceShowcase />;
}
